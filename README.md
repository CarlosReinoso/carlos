use 'pnpm create ' - though better to copy old project to avoid all the setup for tailwind and markdown

crop hero using ffmpeg from the portrait insta `ffmpeg -i input.mp4 -vf "scale=1920:-1, crop=1920:1080" output.mp4
`
